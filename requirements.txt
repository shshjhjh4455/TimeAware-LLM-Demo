# PyTorch (CUDA 12.1)
torch>=2.1.0
torchvision
torchaudio

# Transformers
transformers>=4.35.0,<4.40.0
sentence-transformers

# Google Gemini (primary LLM for verifier + response generation)
google-generativeai>=0.8.0

# OpenAI (used for embeddings in memory_retriever and vector_knowledge_retriever)
openai
tiktoken

# Data processing (numpy 1.x for PyTorch 2.1.0 compatibility)
numpy>=1.24.0,<2.0

# Utilities
python-dotenv
